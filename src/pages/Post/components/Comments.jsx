import styled from 'styled-components'

const CommentsContainer = ({ className, comments }) => {
	return <div className={className}>Коментарии</div>
}

export const Comments = styled(CommentsContainer)``
